{
  "name": "webhooktester",
  "type": "module",
  "scripts": {
    "build-js": "rollup -c",
    "minify-css": "cleancss wwwroot/css/*.css -o wwwroot/bundle.min.css",
    "build": "npm run build-js && npm run minify-css",
    "watch": "rollup -c -w"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-terser": "^0.4.4",
    "clean-css-cli": "^5.6.3",
    "rollup": "^4.21.1"
  }
}